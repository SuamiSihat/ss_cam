<Page x:Class="SS_CAM.Views.TaskManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Task Manager">
    <Page.Resources>
        <Style TargetType="CalendarDayButton">
            <Style.Triggers>
                <Trigger Property="IsToday" Value="True">
                    <Setter Property="Background" Value="{DynamicResource FluentBrand80}" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontWeight" Value="Bold" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header & Metric Quick Glance -->
        <StackPanel Grid.Row="0" Margin="24,14,24,10">
            <!-- Header Title -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <ui:SymbolIcon Symbol="TaskListSquareLtr24" FontSize="24" Foreground="{DynamicResource FluentBrand80}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ui:TextBlock Text="Task Manager" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <ui:TextBlock Text="Monitor, filter, and drag-and-drop update project workflows driven by README.md YAML frontmatter." Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,4,0,12" />

            <!-- Metric Quick Glance Cards (5 KPI Summary Bar) -->
            <UniformGrid Columns="5" Margin="0,0,0,0">
                <!-- Total Projects Card -->
                <ui:Card Padding="12,8" Margin="0,0,8,0">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <ui:SymbolIcon Symbol="Folder24" FontSize="13" Foreground="{DynamicResource FluentBrand80}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="TOTAL PROJECTS" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                        <ui:TextBlock x:Name="MetricTotalProjects" Text="0" Foreground="{DynamicResource FluentBrand80}" FontSize="22" FontWeight="Bold"/>
                        <ui:TextBlock Text="Workspace total" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" Margin="0,1,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- In Progress Card -->
                <ui:Card Padding="12,8" Margin="0,0,8,0">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <ui:SymbolIcon Symbol="Play24" FontSize="13" Foreground="{DynamicResource FluentBrand80}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="IN PROGRESS" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                        <ui:TextBlock x:Name="MetricInProgress" Text="0" Foreground="{DynamicResource FluentBrand80}" FontSize="22" FontWeight="Bold"/>
                        <ui:TextBlock Text="Active WIP tasks" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" Margin="0,1,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- In Review Card -->
                <ui:Card Padding="12,8" Margin="0,0,8,0">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <ui:SymbolIcon Symbol="Eye24" FontSize="13" Foreground="{DynamicResource SystemFillColorCautionBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="IN REVIEW" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                        <ui:TextBlock x:Name="MetricReview" Text="0" Foreground="{DynamicResource SystemFillColorCautionBrush}" FontSize="22" FontWeight="Bold"/>
                        <ui:TextBlock Text="Awaiting feedback" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" Margin="0,1,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Urgent & Overdue Card -->
                <ui:Card Padding="12,8" Margin="0,0,8,0">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <ui:SymbolIcon Symbol="Warning24" FontSize="13" Foreground="{DynamicResource SystemFillColorCriticalBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="URGENT / OVERDUE" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <ui:TextBlock x:Name="MetricUrgent" Text="0" Foreground="{DynamicResource SystemFillColorCriticalBrush}" FontSize="22" FontWeight="Bold"/>
                        <ui:TextBlock Text="Requires priority" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" Margin="0,1,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Completed Card -->
                <ui:Card Padding="12,8">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                            <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="13" Foreground="{DynamicResource SystemFillColorSuccessBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="COMPLETED" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                        <ui:TextBlock x:Name="MetricDone" Text="0" Foreground="{DynamicResource SystemFillColorSuccessBrush}" FontSize="22" FontWeight="Bold"/>
                        <ui:TextBlock Text="Finished projects" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" Margin="0,1,0,0"/>
                    </StackPanel>
                </ui:Card>
            </UniformGrid>
        </StackPanel>

        <!-- Interactive Control & Search Bar -->
        <ui:Card Grid.Row="1" Padding="12,8" Margin="24,0,24,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search & Filters -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Real-time Search Input -->
                    <ui:TextBox x:Name="TxtSearchQuery" Width="220" Height="32" Padding="8,4" VerticalContentAlignment="Center" PlaceholderText="Search project or client..." Icon="Search24" TextChanged="OnTMSearchQueryChanged" FontSize="11" Margin="0,0,12,0"/>

                    <ui:TextBlock Text="Designer:" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="DesignerFilterTM" Width="140" Height="32" Padding="8,4" VerticalContentAlignment="Center" SelectionChanged="OnTMFilterChanged" FontSize="11" Margin="0,0,12,0"/>

                    <ui:TextBlock Text="Priority:" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="PriorityFilter" Width="120" Height="32" Padding="8,4" VerticalContentAlignment="Center" SelectionChanged="OnTMFilterChanged" FontSize="11" Margin="0,0,12,0">
                        <ComboBoxItem Content="All Priorities" IsSelected="True" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="urgent" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="high" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="medium" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="low" VerticalContentAlignment="Center"/>
                    </ComboBox>

                    <ui:TextBlock Text="Sort:" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="SortFilter" Width="160" Height="32" Padding="8,4" VerticalContentAlignment="Center" SelectionChanged="OnTMFilterChanged" FontSize="11" Margin="0,0,12,0">
                        <ComboBoxItem Content="Oldest First (Queue)" IsSelected="True" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="Newest First" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="Deadline (Urgent)" VerticalContentAlignment="Center"/>
                        <ComboBoxItem Content="Priority" VerticalContentAlignment="Center"/>
                    </ComboBox>

                    <ui:Button Appearance="Secondary" Click="OnTMResetFiltersClicked" Height="32" Padding="10,0" Cursor="Hand" FontSize="11" ToolTip="Clear search and filters">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Dismiss24" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="Reset" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                </StackPanel>

                <!-- Actions & Count -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:TextBlock x:Name="TxtTaskCount" Text="0 projects" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <ui:Button Appearance="Primary" Click="OnTMRefreshClicked" Height="32" Padding="14,0" Cursor="Hand" FontSize="11" FontWeight="Bold">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="ArrowSync24" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="Scan Workspace" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </ui:Card>

        <!-- Kanban Board Columns (Header Pinned to Top of Each Column Stack, VerticalContentAlignment Top) -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                      VerticalAlignment="Stretch" Margin="24,0,24,14">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">

                <!-- Backlog Column -->
                <ui:Card Width="260" Margin="0,0,14,0" Padding="0" ClipToBounds="True"
                         VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                         AllowDrop="True" Tag="backlog"
                         DragOver="OnColumnDragOver" DragLeave="OnColumnDragLeave" Drop="OnColumnDrop">
                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Pinned Column Header -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="14,10" VerticalAlignment="Top">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="5" Background="#94A3B8" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="Backlog" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <Border Background="{DynamicResource TextControlBackground}" CornerRadius="8" Padding="6,2" Margin="8,0,0,0">
                                    <ui:TextBlock x:Name="CountBacklog" Text="0" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Scrollable Cards Stack -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="False" Background="Transparent" VerticalAlignment="Stretch">
                            <ItemsControl x:Name="ListBacklog" Margin="8" VerticalAlignment="Top">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:CardAction Margin="0,0,0,8" Padding="12,10" Cursor="Hand"
                                                       Click="OnProjectCardClicked"
                                                       PreviewMouseLeftButtonDown="OnCardPreviewMouseLeftButtonDown"
                                                       MouseMove="OnCardMouseMove">
                                            <StackPanel>
                                                 <ui:TextBlock Text="{Binding Project}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" LineHeight="18"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                    <Border Background="{Binding PriorityColor}" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                                        <ui:TextBlock Text="{Binding Priority}" FontSize="9" FontWeight="SemiBold" Foreground="White"/>
                                                    </Border>
                                                    <Border Background="{Binding DeadlineBadgeBackground}" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                                        <ui:TextBlock Text="{Binding DeadlineDisplay}" FontSize="10" FontWeight="SemiBold" Foreground="{Binding DeadlineBadgeForeground}" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border Background="{DynamicResource TextControlBackground}" CornerRadius="4" Padding="4,1" VerticalAlignment="Center" Margin="0,0,6,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <ui:SymbolIcon Symbol="Calendar24" FontSize="10" Foreground="{Binding AgeBadgeColor}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                            <ui:TextBlock Text="{Binding AgeDisplay}" FontSize="9" FontWeight="SemiBold" Foreground="{Binding AgeBadgeColor}" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <ui:TextBlock Text="{Binding Designer}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text="{Binding CreatedDateDisplay}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <StackPanel Orientation="Horizontal" Visibility="{Binding DurationVisibility}">
                                                        <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                        <ui:SymbolIcon Symbol="Clock24" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <ui:TextBlock Text="{Binding Duration}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </StackPanel>
                                        </ui:CardAction>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>

                <!-- In Progress Column -->
                <ui:Card Width="260" Margin="0,0,14,0" Padding="0" ClipToBounds="True"
                         VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                         AllowDrop="True" Tag="in-progress"
                         DragOver="OnColumnDragOver" DragLeave="OnColumnDragLeave" Drop="OnColumnDrop">
                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Pinned Column Header -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="14,10" VerticalAlignment="Top">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="5" Background="#3B82F6" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="In Progress" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource FluentBrand80}"/>
                                <Border Background="{DynamicResource TextControlBackground}" CornerRadius="8" Padding="6,2" Margin="8,0,0,0">
                                    <ui:TextBlock x:Name="CountInProgress" Text="0" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Scrollable Cards Stack -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="False" Background="Transparent" VerticalAlignment="Stretch">
                            <ItemsControl x:Name="ListInProgress" Margin="8" VerticalAlignment="Top">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:CardAction Margin="0,0,0,8" Padding="12,10" Cursor="Hand"
                                                       Click="OnProjectCardClicked"
                                                       PreviewMouseLeftButtonDown="OnCardPreviewMouseLeftButtonDown"
                                                       MouseMove="OnCardMouseMove">
                                            <StackPanel>
                                                <ui:TextBlock Text="{Binding Project}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" LineHeight="18"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                    <Border Background="{Binding PriorityColor}" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                                        <ui:TextBlock Text="{Binding Priority}" FontSize="9" FontWeight="SemiBold" Foreground="White"/>
                                                    </Border>
                                                    <Border Background="{Binding DeadlineBadgeBackground}" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                                        <ui:TextBlock Text="{Binding DeadlineDisplay}" FontSize="10" FontWeight="SemiBold" Foreground="{Binding DeadlineBadgeForeground}" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border Background="{DynamicResource TextControlBackground}" CornerRadius="4" Padding="4,1" VerticalAlignment="Center" Margin="0,0,6,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <ui:SymbolIcon Symbol="Calendar24" FontSize="10" Foreground="{Binding AgeBadgeColor}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                            <ui:TextBlock Text="{Binding AgeDisplay}" FontSize="9" FontWeight="SemiBold" Foreground="{Binding AgeBadgeColor}" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <ui:TextBlock Text="{Binding Designer}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text="{Binding CreatedDateDisplay}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <StackPanel Orientation="Horizontal" Visibility="{Binding DurationVisibility}">
                                                        <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                        <ui:SymbolIcon Symbol="Clock24" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <ui:TextBlock Text="{Binding Duration}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </StackPanel>
                                        </ui:CardAction>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>

                <!-- Review Column -->
                <ui:Card Width="260" Margin="0,0,14,0" Padding="0" ClipToBounds="True"
                         VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                         AllowDrop="True" Tag="review"
                         DragOver="OnColumnDragOver" DragLeave="OnColumnDragLeave" Drop="OnColumnDrop">
                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Pinned Column Header -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="14,10" VerticalAlignment="Top">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="5" Background="#F59E0B" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="Review" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource SystemFillColorCautionBrush}"/>
                                <Border Background="{DynamicResource TextControlBackground}" CornerRadius="8" Padding="6,2" Margin="8,0,0,0">
                                    <ui:TextBlock x:Name="CountReview" Text="0" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource SystemFillColorCautionBrush}"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Scrollable Cards Stack -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="False" Background="Transparent" VerticalAlignment="Stretch">
                            <ItemsControl x:Name="ListReview" Margin="8" VerticalAlignment="Top">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:CardAction Margin="0,0,0,8" Padding="12,10" Cursor="Hand"
                                                       Click="OnProjectCardClicked"
                                                       PreviewMouseLeftButtonDown="OnCardPreviewMouseLeftButtonDown"
                                                       MouseMove="OnCardMouseMove">
                                            <StackPanel>
                                                <ui:TextBlock Text="{Binding Project}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" LineHeight="18"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <ui:TextBlock Text="Rev " FontSize="9" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                            <ui:TextBlock Text="{Binding Revision}" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                                        </StackPanel>
                                                    </Border>
                                                    <Border Background="{Binding DeadlineBadgeBackground}" CornerRadius="4" Padding="5,2" Margin="0,0,6,0">
                                                        <ui:TextBlock Text="{Binding DeadlineDisplay}" FontSize="10" FontWeight="SemiBold" Foreground="{Binding DeadlineBadgeForeground}" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border Background="{DynamicResource TextControlBackground}" CornerRadius="4" Padding="4,1" VerticalAlignment="Center" Margin="0,0,6,0">
                                                        <StackPanel Orientation="Horizontal">
                                                            <ui:SymbolIcon Symbol="Calendar24" FontSize="10" Foreground="{Binding AgeBadgeColor}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                            <ui:TextBlock Text="{Binding AgeDisplay}" FontSize="9" FontWeight="SemiBold" Foreground="{Binding AgeBadgeColor}" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <ui:TextBlock Text="{Binding Designer}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text="{Binding CreatedDateDisplay}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <StackPanel Orientation="Horizontal" Visibility="{Binding DurationVisibility}">
                                                        <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                        <ui:SymbolIcon Symbol="Clock24" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,3,0" VerticalAlignment="Center"/>
                                                        <ui:TextBlock Text="{Binding Duration}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </StackPanel>
                                        </ui:CardAction>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>

                <!-- Done Column -->
                <ui:Card Width="260" Margin="0,0,14,0" Padding="0" ClipToBounds="True"
                         VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                         AllowDrop="True" Tag="done"
                         DragOver="OnColumnDragOver" DragLeave="OnColumnDragLeave" Drop="OnColumnDrop">
                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Pinned Column Header -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="14,10" VerticalAlignment="Top">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="5" Background="#10B981" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="Done" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>
                                <Border Background="{DynamicResource TextControlBackground}" CornerRadius="8" Padding="6,2" Margin="8,0,0,0">
                                    <ui:TextBlock x:Name="CountDone" Text="0" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Scrollable Cards Stack -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="False" Background="Transparent" VerticalAlignment="Stretch">
                            <ItemsControl x:Name="ListDone" Margin="8" VerticalAlignment="Top">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:CardAction Margin="0,0,0,8" Padding="12,10" Cursor="Hand"
                                                       Click="OnProjectCardClicked"
                                                       PreviewMouseLeftButtonDown="OnCardPreviewMouseLeftButtonDown"
                                                       MouseMove="OnCardMouseMove">
                                            <StackPanel>
                                                <ui:TextBlock Text="{Binding Project}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" LineHeight="18" TextDecorations="Strikethrough" Opacity="0.75"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <ui:TextBlock Text="{Binding Client}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text="{Binding AgeDisplay}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </ui:CardAction>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>

                <!-- On Hold / Untracked Column -->
                <ui:Card Width="260" Padding="0" ClipToBounds="True"
                         VerticalAlignment="Stretch" VerticalContentAlignment="Top"
                         AllowDrop="True" Tag="on-hold"
                         DragOver="OnColumnDragOver" DragLeave="OnColumnDragLeave" Drop="OnColumnDrop">
                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Pinned Column Header -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="14,10" VerticalAlignment="Top">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" CornerRadius="5" Background="#94A3B8" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="On Hold / Untracked" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <Border Background="{DynamicResource TextControlBackground}" CornerRadius="8" Padding="6,2" Margin="8,0,0,0">
                                    <ui:TextBlock x:Name="CountOther" Text="0" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Scrollable Cards Stack -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="False" Background="Transparent" VerticalAlignment="Stretch">
                            <ItemsControl x:Name="ListOther" Margin="8" VerticalAlignment="Top">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ui:CardAction Margin="0,0,0,8" Padding="12,10" Cursor="Hand"
                                                       Click="OnProjectCardClicked"
                                                       PreviewMouseLeftButtonDown="OnCardPreviewMouseLeftButtonDown"
                                                       MouseMove="OnCardMouseMove">
                                            <StackPanel>
                                                <ui:TextBlock Text="{Binding Project}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" LineHeight="18" Opacity="0.75"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <ui:TextBlock Text="{Binding StatusDisplay}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text=" &#x2022; " FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text="{Binding AgeDisplay}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </ui:CardAction>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Project Detail Overlay (Edit panel drawer - full height expanded README notes down to Save button) -->
        <Border x:Name="DetailPanel" Grid.Row="0" Grid.RowSpan="3"
                Width="400" HorizontalAlignment="Right"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1,0,0,0"
                Visibility="Collapsed">
            <DockPanel Margin="20" LastChildFill="True">
                <!-- Close Button -->
                <ui:Button DockPanel.Dock="Top" Click="OnDetailClose" HorizontalAlignment="Right"
                           Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,0,12" ToolTip="Close Panel">
                    <ui:SymbolIcon Symbol="Dismiss24" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                </ui:Button>

                <!-- Project Title -->
                <ui:TextBlock DockPanel.Dock="Top" x:Name="DetailProjectName"
                             FontSize="16" FontWeight="Bold"
                             Foreground="{DynamicResource FluentBrand80}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                <!-- Compact 3x2 Form Metadata Row -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Status -->
                    <StackPanel Grid.Row="0" Grid.Column="0">
                        <ui:TextBlock Text="Status" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                        <ComboBox x:Name="DetailStatus" Height="36" FontSize="12" Padding="8,4" VerticalContentAlignment="Center">
                            <ComboBoxItem Content="backlog" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="in-progress" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="review" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="done" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="on-hold" VerticalContentAlignment="Center"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Priority -->
                    <StackPanel Grid.Row="0" Grid.Column="2">
                        <ui:TextBlock Text="Priority" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                        <ComboBox x:Name="DetailPriority" Height="36" FontSize="12" Padding="8,4" VerticalContentAlignment="Center">
                            <ComboBoxItem Content="low" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="medium" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="high" VerticalContentAlignment="Center"/>
                            <ComboBoxItem Content="urgent" VerticalContentAlignment="Center"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Deadline -->
                    <StackPanel Grid.Row="2" Grid.Column="0">
                        <ui:TextBlock Text="Deadline (YYYY-MM-DD)" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                        <DatePicker x:Name="DetailDeadline" Height="36" FontSize="12" Padding="4" VerticalContentAlignment="Center" SelectedDateChanged="OnDateChanged"/>
                    </StackPanel>

                    <!-- Created Date -->
                    <StackPanel Grid.Row="2" Grid.Column="2">
                        <ui:TextBlock Text="Created Date (YYYY-MM-DD)" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                        <DatePicker x:Name="DetailCreatedDate" Height="36" FontSize="12" Padding="4" VerticalContentAlignment="Center" SelectedDateChanged="OnDateChanged"/>
                    </StackPanel>

                    <!-- Task Duration -->
                    <StackPanel Grid.Row="4" Grid.Column="0">
                        <ui:TextBlock Text="Duration (e.g. 3d, 5h)" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                        <ui:TextBox x:Name="DetailDuration" Height="36" FontSize="12" Padding="8,4" VerticalContentAlignment="Center" PlaceholderText="e.g. 3d"/>
                    </StackPanel>

                    <!-- Revision with Stepper Controls (+ / -) -->
                    <StackPanel Grid.Row="4" Grid.Column="2">
                        <ui:TextBlock Text="Revision Round" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ui:Button Grid.Column="0" Click="OnRevisionDecrementClicked" Appearance="Secondary" Height="36" Width="36" Padding="0" Cursor="Hand" ToolTip="Decrease revision count">
                                <ui:SymbolIcon Symbol="Subtract24" FontSize="12" VerticalAlignment="Center"/>
                            </ui:Button>
                            <ui:TextBox Grid.Column="1" x:Name="DetailRevision" Height="36" FontSize="12" Padding="4,4" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Margin="4,0" Text="0"/>
                            <ui:Button Grid.Column="2" Click="OnRevisionIncrementClicked" Appearance="Secondary" Height="36" Width="36" Padding="0" Cursor="Hand" ToolTip="Increase revision count">
                                <ui:SymbolIcon Symbol="Add24" FontSize="12" VerticalAlignment="Center"/>
                            </ui:Button>
                        </Grid>
                    </StackPanel>
                </Grid>

                <!-- Save Button & Status Feedback (Centered at Bottom) -->
                <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,12,0,0">
                    <ui:Button Appearance="Primary" Click="OnDetailSave" Height="36" Padding="20,0" FontWeight="Bold" Cursor="Hand" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Save24" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="Save to README.md" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                    <ui:TextBlock x:Name="DetailSaveStatus" Text="" FontSize="11" Foreground="{DynamicResource SystemFillColorSuccessBrush}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <!-- README Content Notes Header & Grouped Action Toolbar on Right -->
                <Grid DockPanel.Dock="Top" Margin="0,0,0,6">
                    <ui:TextBlock Text="README Notes" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                    <!-- Grouped Action Buttons on the Right -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <!-- Icon-Only Preview & Edit Switcher -->
                        <Border Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="4" Padding="2" Margin="0,0,6,0">
                            <StackPanel Orientation="Horizontal">
                                <ui:Button x:Name="BtnModePreview" Click="OnReadmeModePreviewClicked" Appearance="Primary" Padding="6,4" Height="26" Width="30" ToolTip="Preview Rendered Markdown">
                                    <ui:SymbolIcon Symbol="Eye24" FontSize="12" VerticalAlignment="Center"/>
                                </ui:Button>
                                <ui:Button x:Name="BtnModeEdit" Click="OnReadmeModeEditClicked" Appearance="Secondary" Padding="6,4" Height="26" Width="30" ToolTip="Edit Markdown Notes">
                                    <ui:SymbolIcon Symbol="Edit24" FontSize="12" VerticalAlignment="Center"/>
                                </ui:Button>
                            </StackPanel>
                        </Border>

                        <!-- Icon-Only Open External Raw File Button -->
                        <ui:Button Click="OnOpenRawReadmeClicked" Appearance="Secondary" Padding="4" Width="28" Height="26" Cursor="Hand" ToolTip="Open raw README.md in external text editor">
                            <ui:SymbolIcon Symbol="Open24" FontSize="12" VerticalAlignment="Center"/>
                        </ui:Button>
                    </StackPanel>
                </Grid>

                <!-- README Content Preview Area (Expands to Fill ALL Vertical Space down to Save Button!) -->
                <Grid VerticalAlignment="Stretch">
                    <!-- Rendered Markdown View (Default) -->
                    <FlowDocumentScrollViewer x:Name="DetailReadmeRendered" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="Visible" Margin="0"/>
                    <!-- Raw Text Editor -->
                    <ui:TextBox x:Name="DetailReadmePreview" FontSize="12" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" Visibility="Collapsed" PlaceholderText="No notes/content below frontmatter in README.md"/>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</Page>
