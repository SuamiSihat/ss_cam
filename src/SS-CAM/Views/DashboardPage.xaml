<Page x:Class="SS_CAM.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Dashboard">

    <ScrollViewer x:Name="MainScrollViewer" VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" Padding="24" Background="Transparent" PreviewMouseWheel="OnScrollViewerPreviewMouseWheel">
        <StackPanel>
            <!-- Fluent 2 Page Header Bar -->
            <Grid Margin="0,0,0,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <ui:SymbolIcon Symbol="Home24" FontSize="24" Foreground="{DynamicResource FluentBrand80}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ui:TextBlock Text="Designer Intelligence Dashboard" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                        <Border Background="{DynamicResource FluentBrand80}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Margin="10,0,12,0"
                                Cursor="Hand" ToolTip="Click to open About SS-CAM &amp; Release Changelog" MouseLeftButtonDown="OnVersionBadgeClicked">
                            <ui:TextBlock x:Name="TxtVersionBadge" Text="" FontSize="10.5" FontWeight="Bold" Foreground="White"/>
                        </Border>
                    </StackPanel>
                    <ui:TextBlock Text="Real-time workspace overview, creative KPI metrics, and project telemetry" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="13" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Folder24" FontSize="14" Foreground="{DynamicResource FluentBrand80}" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <ui:TextBlock x:Name="TxtWorkspacePath" Text="" Foreground="{DynamicResource FluentBrand80}" FontWeight="SemiBold" FontSize="12"/>
                    </StackPanel>
                    <ui:TextBlock x:Name="TxtStatus" Text="Ready." Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <ui:Button Appearance="Primary" Click="OnQuickNewProjectClicked" Height="36" Padding="14,0" Margin="0,0,8,0" Cursor="Hand" ToolTip="Scaffold New Project">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Add24" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="New Project" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>

                    <ui:Button Appearance="Secondary" Click="OnQuickCatalogClicked" Height="36" Padding="14,0" Margin="0,0,8,0" Cursor="Hand" ToolTip="Open Project Catalog">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="FolderSearch24" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="Project Catalog" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>

                    <ui:Button Appearance="Secondary" Click="OnRefreshClicked" Width="36" Height="36" Padding="0" Cursor="Hand" ToolTip="Refresh Workspace Analytics">
                        <ui:SymbolIcon Symbol="ArrowSync24" FontSize="16" VerticalAlignment="Center"/>
                    </ui:Button>
                </StackPanel>
            </Grid>

            <!-- Fluent 2 Key Metric Cards Grid (Depth 4 Elevation) - 4-up, 2 rows, exact uniform height -->
            <UniformGrid Rows="2" Columns="4" Height="224" Margin="-6,0,-6,24">
                <!-- Total Projects -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="TOTAL PROJECTS" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="MetricTotalProjects" Text="0" Foreground="{DynamicResource FluentBrand80}" FontSize="28" FontWeight="Bold" Margin="0,2,0,0"/>
                        <ui:TextBlock Text="Workspace total" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Active WIP Projects -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="ACTIVE WIP (7 DAYS)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="MetricActiveWIP" Text="0" Foreground="{DynamicResource SystemFillColorCautionBrush}" FontSize="28" FontWeight="Bold" Margin="0,2,0,0"/>
                        <ui:TextBlock Text="Modified recently" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Latest Project -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="LATEST CREATED" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="MetricLatestProject" Text="-" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontSize="22" FontWeight="Bold" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                        <ui:TextBlock x:Name="MetricLatestProjectSubtext" Text="Most recent project" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </ui:Card>

                <!-- Storage Size -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="STORAGE SIZE" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="MetricFileSize" Text="0 MB" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontSize="22" FontWeight="Bold" Margin="0,2,0,0"/>
                        <ui:TextBlock Text="Total asset volume" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Created This Month -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="THIS MONTH OUTPUT" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="MetricThisMonth" Text="0" Foreground="{DynamicResource SystemFillColorSuccessBrush}" FontSize="28" FontWeight="Bold" Margin="0,2,0,0"/>
                        <ui:TextBlock x:Name="MetricMonthComparison" Text="&#x25B2; +0% vs last month" Foreground="{DynamicResource SystemFillColorSuccessBrush}" FontSize="11" FontWeight="Bold" Margin="0,2,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Largest Project -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="LARGEST PROJECT" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="MetricLargestProjectSize" Text="0 MB" Foreground="{DynamicResource SystemFillColorCautionBrush}" FontSize="22" FontWeight="Bold" Margin="0,2,0,0"/>
                        <ui:TextBlock x:Name="MetricLargestProjectName" Text="None" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </ui:Card>

                <!-- Stale Projects -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="STALE PROJECTS (&gt;90 DAYS)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                        <ui:TextBlock x:Name="MetricStaleProjects" Text="0" Foreground="{DynamicResource SystemFillColorCriticalBrush}" FontSize="28" FontWeight="Bold" Margin="0,2,0,0"/>
                        <ui:TextBlock Text="Idle folders to archive" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </ui:Card>

                <!-- Flow Metrics -->
                <ui:Card Padding="16,12" Margin="6" VerticalAlignment="Stretch" Effect="{DynamicResource FluentDepth4}">
                    <StackPanel VerticalAlignment="Center">
                        <ui:TextBlock Text="CREATIVE TEAM FLOW" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" FontWeight="Bold"/>
                        <ui:TextBlock x:Name="FlowDesignerCount" Text="0 Designers, 0 Projects" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                        <ui:TextBlock x:Name="FlowFileCount" Text="0 files indexed" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </ui:Card>
            </UniformGrid>

            <!-- Real-Time Live Studio Tasks (60-30-10 Fluent 2) -->
            <ui:Card Margin="0,0,0,24" Padding="20" Effect="{DynamicResource FluentDepth4}">
                <StackPanel>
                    <!-- Header with Real-time Pulsing Badge -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="28" Height="28" CornerRadius="6" Background="{DynamicResource FluentBrandLight}" Margin="0,0,10,0" VerticalAlignment="Center">
                                <ui:TextBlock Text="&#xE916;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="{DynamicResource FluentBrand80}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <ui:TextBlock Text="Live Studio Tasks" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                                    <Border x:Name="LiveActiveBadge" Background="#E8F8F0" BorderBrush="#10B981" BorderThickness="1" CornerRadius="10" Padding="8,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="6" Height="6" Fill="#10B981" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <ui:TextBlock x:Name="TxtLiveActiveCount" Text="0 Active" FontSize="10.5" FontWeight="Bold" Foreground="#10B981" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                                <ui:TextBlock Text="Real-time studio designer work telemetry &amp; live session durations" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,0,8,0" VerticalAlignment="Center">
                                <ui:SymbolIcon Symbol="Person24" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <ComboBox x:Name="CmbLiveDesignerFilter" SelectionChanged="OnLiveDesignerFilterChanged"
                                          Height="36" Width="140" Padding="10,0" VerticalContentAlignment="Center" FontSize="11.5"
                                          ToolTip="Filter active studio tasks by designer">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <ui:TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </StackPanel>
                            <ui:Button Appearance="Secondary" Click="OnRefreshLiveTasksClicked" Height="32" Padding="10,0" Cursor="Hand" ToolTip="Refresh Live Tasks">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="ArrowSync24" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                    <ui:TextBlock Text="Sync Live Stream" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ui:Button>
                        </StackPanel>
                    </Grid>

                    <!-- Active Live Task Cards List -->
                    <ItemsControl x:Name="LiveTasksControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Card Padding="14" Margin="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Designer Avatar Circle -->
                                        <Border Width="40" Height="40" CornerRadius="20" Background="{DynamicResource FluentBrandLight}" Margin="0,0,12,0" VerticalAlignment="Center">
                                            <ui:TextBlock Text="{Binding Initials}" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Project & Designer Details -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                                <ui:TextBlock Text="{Binding DisplayAuthor}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,8,0" TextTrimming="CharacterEllipsis"/>
                                                <Border Background="{Binding StatusBrush}" CornerRadius="4" Padding="6,1" VerticalAlignment="Center">
                                                    <ui:TextBlock Text="{Binding StatusLabel}" Foreground="White" FontSize="9" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>

                                            <!-- Project Name -->
                                            <ui:TextBlock Text="{Binding ProjectName}" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource FluentBrand80}" TextTrimming="CharacterEllipsis" Margin="0,0,0,2"/>

                                            <!-- Activity Notes if any -->
                                            <ui:TextBlock Text="{Binding SessionNotes}" FontSize="10.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <!-- Right: Real-time Stopwatch & Action Button -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                            <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="6" Padding="10,4" Margin="0,0,0,6">
                                                <ui:TextBlock Text="{Binding FormattedTime}" FontFamily="Consolas, Segoe UI" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center"/>
                                            </Border>
                                            <ui:Button Appearance="Secondary" Height="28" Padding="10,0" Click="OnLiveTaskOpenProjectClicked" Tag="{Binding ProjectId}" Cursor="Hand" ToolTip="Open in Project Catalog">
                                                <StackPanel Orientation="Horizontal">
                                                    <ui:SymbolIcon Symbol="Open24" FontSize="11" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                    <ui:TextBlock Text="View Project" FontSize="10.5" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </ui:Button>
                                        </StackPanel>
                                    </Grid>
                                </ui:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Friendly Empty State when all workstations are idle -->
                    <Border x:Name="PanelNoLiveTasks" Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="8" Padding="24,18" Visibility="Collapsed">
                        <StackPanel HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,6">
                                <ui:SymbolIcon Symbol="Timer24" FontSize="16" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="All Studio Workstations Idle" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ui:TextBlock Text="Start a project work timer (Ctrl + K or header pill) to broadcast live creative focus to the studio team." FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ui:Card>

            <!-- Inline 6 Last Created Projects List (Fluent 2 Card Container) -->
            <ui:Card Margin="0,0,0,24" Padding="20" Effect="{DynamicResource FluentDepth4}">
                <StackPanel>
                    <ui:TextBlock Text="Recent Past Projects (Last 6 Created)" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,16"/>
                    <ItemsControl x:Name="Recent6ProjectsControl" Margin="0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card Padding="12" Margin="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Width="34" Height="34" CornerRadius="6" Background="{DynamicResource FluentBrandLight}" Margin="0,0,10,0" VerticalAlignment="Center">
                                        <ui:TextBlock Text="&#xED25;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FluentBrand80}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <ui:TextBlock Text="{Binding Project}" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <Border Background="{DynamicResource FluentBrand80}" CornerRadius="4" Padding="6,1" Margin="0,0,6,0">
                                                <ui:TextBlock Text="{Binding Designer}" Foreground="White" FontSize="9.5" FontWeight="Bold"/>
                                            </Border>
                                            <ui:TextBlock Text="{Binding Modified}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="10" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:Card>

            <!-- Designer Workload & Capacity Section -->
            <ui:Card Margin="0,0,0,24" Padding="20" Effect="{DynamicResource FluentDepth4}">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="PeopleTeam24" FontSize="16" Foreground="{DynamicResource FluentBrand80}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <ui:TextBlock Text="Designer Workload &amp; Capacity Radar" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ui:TextBlock Grid.Column="1" Text="Max 5.0 creative units recommended" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                    </Grid>

                    <ItemsControl x:Name="DesignerWorkloadControl">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Card Padding="14" Margin="4">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <ui:TextBlock Text="{Binding DesignerName}" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                                <StackPanel Orientation="Horizontal">
                                                    <ui:TextBlock Text="{Binding StaffId}" FontSize="10.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                    <ui:TextBlock Text=" &#x2022; " FontSize="10.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                    <ui:TextBlock Text="{Binding ActiveWeightDisplay}" FontSize="10.5" FontWeight="SemiBold" Foreground="{DynamicResource FluentBrand80}"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <Border Grid.Column="1" Background="{DynamicResource FluentBrandLight}" CornerRadius="4" Padding="6,2" VerticalAlignment="Top">
                                                <ui:TextBlock Text="{Binding CapacityStatus}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}"/>
                                            </Border>
                                        </Grid>

                                        <!-- Capacity Progress Bar -->
                                        <ProgressBar Value="{Binding CapacityPercent}" Maximum="100" Height="6" Margin="0,10,0,8" Foreground="{DynamicResource FluentBrand80}"/>

                                        <Grid Margin="0,2,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <ui:TextBlock Text="In-Progress" FontSize="9.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                <ui:TextBlock Text="{Binding InProgressCount}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                                <ui:TextBlock Text="Review / Rev" FontSize="9.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                <ui:TextBlock Text="{Binding ReviewCount}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource SystemFillColorCautionBrush}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                <ui:TextBlock Text="Completed" FontSize="9.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                <ui:TextBlock Text="{Binding DoneCount}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource SystemFillColorSuccessBrush}"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </ui:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:Card>

            <!-- Creative SLA & Revision Turnaround Analytics -->
            <ui:Card Margin="0,0,0,24" Padding="20" Effect="{DynamicResource FluentDepth4}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <ui:SymbolIcon Symbol="Timer24" FontSize="16" Foreground="{DynamicResource FluentBrand80}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <ui:TextBlock Text="Operational SLA &amp; Revision Turnaround Velocity" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ui:Card Grid.Column="0" Margin="0,0,8,0" Padding="14">
                            <StackPanel>
                                <ui:TextBlock Text="FIRST-TIME RIGHT RATE" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBlock x:Name="TxtSlaFirstTimeRight" Text="85.0%" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource SystemFillColorSuccessBrush}" Margin="0,4,0,2"/>
                                <ui:TextBlock Text="Projects signed off with 0 revisions" FontSize="10.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </ui:Card>

                        <ui:Card Grid.Column="1" Margin="4,0,4,0" Padding="14">
                            <StackPanel>
                                <ui:TextBlock Text="AVG TURNAROUND VELOCITY" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBlock x:Name="TxtSlaAvgTurnaround" Text="3.5 Days" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}" Margin="0,4,0,2"/>
                                <ui:TextBlock Text="From brief creation to deliverable sign-off" FontSize="10.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </ui:Card>

                        <ui:Card Grid.Column="2" Margin="8,0,0,0" Padding="14">
                            <StackPanel>
                                <ui:TextBlock Text="AVG REVISION ROUNDS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBlock x:Name="TxtSlaAvgRevs" Text="0.4 Revs" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,4,0,2"/>
                                <ui:TextBlock Text="Average iterations per deliverable" FontSize="10.5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </ui:Card>
                    </Grid>
                </StackPanel>
            </ui:Card>

            <!-- Hidden chart controls for data binding compatibility -->
            <StackPanel Visibility="Collapsed">
                <ItemsControl x:Name="TypeChartControl"/>
                <ItemsControl x:Name="BrandChartControl"/>
                <ItemsControl x:Name="StorageChartControl"/>
                <ItemsControl x:Name="ActivityChartControl"/>
            </StackPanel>

            <!-- Designer Inspiration Widget -->
            <ui:Card Padding="20" Margin="0,0,0,24" Effect="{DynamicResource FluentDepth4}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: Tip Display -->
                    <StackPanel Grid.Column="0" Margin="0,0,24,0">
                        <!-- Card Header -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <Border Background="{DynamicResource FluentBrandLight}" CornerRadius="6" Width="34" Height="34" Margin="0,0,10,0">
                                <ui:TextBlock x:Name="TxtInsightIcon" Text="&#xE82F;" FontFamily="Segoe Fluent Icons" FontSize="16" Foreground="{DynamicResource FluentBrand80}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <ui:TextBlock Text="DESIGNER INSIGHT" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                <ui:TextBlock x:Name="TxtInsightMode" Text="Tip of the Day" FontSize="11" Foreground="{DynamicResource FluentBrand80}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Tip Content -->
                        <StackPanel x:Name="PanelTipContent">
                            <ui:TextBlock x:Name="TxtTipTitle" Text="Loading tip..." FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6" TextWrapping="Wrap"/>
                            <ui:TextBlock x:Name="TxtTipBody" Text="" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" LineHeight="18"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Right: Controls -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right" Width="120">
                        <ui:Button x:Name="BtnNextTip" Appearance="Primary" Click="OnNextTipClicked" Height="32" Cursor="Hand" FontWeight="Bold" FontSize="11" Margin="0,0,0,8" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal">
                                <ui:TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <ui:TextBlock Text="Next Tip" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ui:Button>
                        <ui:TextBlock x:Name="TxtTipCounter" Text="1 / 40" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </ui:Card>

            <!-- - Team Board Card - -->
            <ui:Card Padding="20" Margin="0,16,0,0">
                <StackPanel>
                    <!-- Header -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <ui:TextBlock Text="&#xE716;" FontFamily="Segoe Fluent Icons" FontSize="18" Foreground="{DynamicResource FluentBrand80}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ui:TextBlock Text="Team Board" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}"/>
                        </StackPanel>
                        <ui:TextBlock Grid.Column="1" x:Name="TxtTeamBoardStatus" Text="" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Notes Feed -->
                    <ItemsControl x:Name="TeamNoteFeed" Margin="0,0,0,12">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Card Margin="0,0,0,6" Padding="10,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                                <ui:TextBlock Text="{Binding Author}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource FluentBrand80}" Margin="0,0,8,0"/>
                                                <ui:TextBlock Text="{Binding DisplayTimestamp}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                            </StackPanel>
                                            <ui:TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                                            <ui:Button Tag="{Binding Id}" Appearance="Transparent" Click="OnPinNoteClicked" Cursor="Hand" Padding="4" ToolTip="{Binding PinTooltip}">
                                                <ui:TextBlock Text="{Binding PinIcon}" FontFamily="Segoe Fluent Icons" FontSize="13" Foreground="{DynamicResource FluentBrand80}"/>
                                            </ui:Button>
                                            <ui:Button Tag="{Binding Id}" Appearance="Transparent" Click="OnDeleteTeamNoteClicked" Cursor="Hand" Padding="4">
                                                <ui:TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                            </ui:Button>
                                        </StackPanel>
                                    </Grid>
                                </ui:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Post Note Row -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6" Margin="0,0,8,0">
                            <Grid>
                                <TextBox x:Name="NewTeamNoteInput" Height="36" Padding="10,8" BorderThickness="0" Background="Transparent" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <ui:TextBlock IsHitTestVisible="False" Text="Post a note to the team..." VerticalAlignment="Center" Margin="11,0,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                    <ui:TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=NewTeamNoteInput}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:TextBlock.Style>
                                </ui:TextBlock>
                            </Grid>
                        </Border>
                        <ui:Button Grid.Column="1" Appearance="Primary" Click="OnPostTeamNoteClicked" Width="36" Height="36" Padding="0" Cursor="Hand" ToolTip="Post Team Announcement">
                            <ui:SymbolIcon Symbol="Send24" FontSize="16" VerticalAlignment="Center"/>
                        </ui:Button>
                    </Grid>
                </StackPanel>
            </ui:Card>

        </StackPanel>
    </ScrollViewer>
</Page>




