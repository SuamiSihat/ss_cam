using System;
using System.Windows;
using System.Windows.Controls;
using SS_CAM.Services;

namespace SS_CAM.Views
{
    /// <summary>
    /// {{PAGE_TITLE}} — {{PAGE_SUBTITLE}}
    /// </summary>
    public partial class {{PAGE_NAME}}Page : Page
    {
        public {{PAGE_NAME}}Page()
        {
            InitializeComponent();
            Loaded   += OnLoaded;
            Unloaded += OnUnloaded;
        }

        private void OnLoaded(object sender, RoutedEventArgs e)
        {
            try
            {
                RefreshData();
            }
            catch (Exception ex)
            {
                System.Diagnostics.Debug.WriteLine($"[{{PAGE_NAME}}Page.OnLoaded] {ex.Message}");
            }
        }

        private void OnUnloaded(object sender, RoutedEventArgs e)
        {
            // Clean up timers, subscriptions, and resources here
        }

        private void RefreshData()
        {
            // Load and bind data here
        }

        private void OnRefreshClicked(object sender, RoutedEventArgs e)
        {
            try
            {
                RefreshData();
            }
            catch (Exception ex)
            {
                System.Diagnostics.Debug.WriteLine($"[{{PAGE_NAME}}Page.OnRefreshClicked] {ex.Message}");
            }
        }
    }
}